"""
Конфигурация бота. Значения берутся из переменных окружения (.env),
чтобы токены и секреты не попадали в код / git.
"""

import os
from dotenv import load_dotenv

load_dotenv()

# Токен, который вы сами придумываете и указываете при настройке
# webhook в кабинете Meta for Developers (любая строка, например: "myBotVerify2026")
VERIFY_TOKEN = os.getenv("IG_VERIFY_TOKEN", "")

# Page Access Token, полученный через Graph API Explorer / OAuth
PAGE_ACCESS_TOKEN = os.getenv("IG_PAGE_ACCESS_TOKEN", "")

# Instagram Business Account ID — получен в Meta for Developers при
# подключении Instagram business login (App Dashboard → Instagram →
# API setup with Instagram business login)
IG_USER_ID = os.getenv("IG_USER_ID", "")

# Версия Graph API (проверяйте актуальную в документации Meta)
GRAPH_API_VERSION = os.getenv("IG_GRAPH_API_VERSION", "v21.0")

# Минимальный интервал (в секундах) между повторными ответами одному и
# тому же человеку — защита от спама, если кто-то присылает триггерное
# слово много раз подряд. По умолчанию 1 час (3600 секунд).
REPLY_COOLDOWN_SECONDS = int(os.getenv("REPLY_COOLDOWN_SECONDS", "3600"))

# ---------------------------------------------------------------------------
# Оповещения о падении сервиса через ваш существующий Telegram-бот —
# отправка сообщения через Bot API, без изменений в коде самого бота.
# ---------------------------------------------------------------------------
TELEGRAM_ALERT_BOT_TOKEN = os.getenv("TELEGRAM_ALERT_BOT_TOKEN", "")
TELEGRAM_ALERT_CHAT_ID = os.getenv("TELEGRAM_ALERT_CHAT_ID", "")

# ---------------------------------------------------------------------------
# Проверка статуса через UptimeRobot API — используется вместо платных
# Alert Contacts (Webhook/Telegram), которые недоступны на бесплатном
# тарифе. API для чтения статуса монитора бесплатен на всех тарифах.
# ---------------------------------------------------------------------------
UPTIMEROBOT_API_KEY = os.getenv("UPTIMEROBOT_API_KEY", "")
# Отдельный секрет для защиты эндпоинта проверки (может совпадать с
# ALERT_WEBHOOK_SECRET, но лучше держать разными на всякий случай).
CRON_SECRET = os.getenv("CRON_SECRET", "")
# Как часто (в секундах) можно повторно слать алерт, пока монитор всё ещё
# лежит — чтобы не засыпать Telegram сообщениями при каждой проверке.
UPTIME_ALERT_COOLDOWN_SECONDS = int(os.getenv("UPTIME_ALERT_COOLDOWN_SECONDS", "1800"))


# ---------------------------------------------------------------------------
# Ключевые слова / фразы для нечёткого срабатывания бота.
# Совпадение ищется как ПОДСТРОКА в тексте (без учёта регистра), поэтому
# бот реагирует не только на точное слово, но и на однокоренные формы,
# опечатки и фразы вроде "и что там про меня".
# Дополняйте список по мере того, как видите новые формулировки в реальных
# комментариях и сообщениях.
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# Ключевые слова / фразы для нечёткого срабатывания бота, разбитые на
# ГРУППЫ. У каждой группы — свой текст ответа в Direct и своя ссылка
# (см. DM_TEXTS ниже). Совпадение ищется как ПОДСТРОКА в тексте (без учёта
# регистра), поэтому бот реагирует не только на точное слово, но и на
# однокоренные формы, опечатки и фразы вроде "и что там про меня".
# Дополняйте списки по мере того, как видите новые формулировки в реальных
# комментариях и сообщениях.
# ---------------------------------------------------------------------------

TRIGGER_KEYWORDS_POSLANIE = [
    # само слово и его формы/опечатки
    "послан",       # послание, пошли, послал(а) — покрывает основные формы
    "псослан",
    "посл ание",
    "poslanje",
    "псл",
    "пслание",
    "послани",
    "ослание",
    # смежные запросы про ожидание / очередь
    "жду",
    "ждю",
    "жд у",
    "когда до меня",
    "когда очередь",
    "очеред",
    "что там про меня",
    "и что там",
    "что же там",
    "про меня",
    # реакции при получении контента
    "здравствуй",
    "добрый вечер",
    "доброй ночи",
    "интересно",
    "посмотри",
    # подтверждение подписки/действия
    "подписал",
    "подписч",
    "готово",
    "считайте",
    "считаете",
    "считай",
    "попробуй",
    "благодарна",
    "благодарен",
]

TRIGGER_KEYWORDS_SPOSOBNOSTI = [
    "способност",   # способности, способность, способностей — все формы
    "суперспособност",
    "спообност",    # частая опечатка
]

TRIGGER_KEYWORDS_CHAKRY = [
    "чакр",         # чакры, чакра, чакрам, чакрах — все формы
    "chakra",
]


def match_trigger_group(text: str) -> str | None:
    """
    Определяет, к какой группе триггеров относится текст: "chakry",
    "sposobnosti", "poslanie", или None, если ничего не совпало.
    Порядок проверки важен — более специфичные группы (чакры, способности)
    проверяются раньше общей группы "послание".
    """
    if not text:
        return None
    lowered = text.lower()
    if any(k in lowered for k in TRIGGER_KEYWORDS_CHAKRY):
        return "chakry"
    if any(k in lowered for k in TRIGGER_KEYWORDS_SPOSOBNOSTI):
        return "sposobnosti"
    if any(k in lowered for k in TRIGGER_KEYWORDS_POSLANIE):
        return "poslanie"
    return None


def text_matches_trigger(text: str) -> bool:
    """Оставлено для обратной совместимости — True, если сработала любая группа."""
    return match_trigger_group(text) is not None


# ---------------------------------------------------------------------------
# Ответы в Direct — свой текст и своя ссылка для каждой группы триггеров.
# ---------------------------------------------------------------------------
CHANNEL_WELCOME_LINK = "https://t.me/proraskritie/24"
CHAKRY_LINK = "https://t.me/proraskritie/37"

AUTO_REPLY_DM_TEXT_POSLANIE = (
    "Привет 🤍 Твой запрос на послание принят и записан.\n\n"
    "Я смотрю каждое послание лично, поэтому очередь двигается "
    "небыстро — сейчас это может занять не один месяц, честно "
    "предупреждаю 🪽\n\n"
    "Пока ждёшь, загляни сюда — там видео про то, какие сверхспособности "
    "уже есть в тебе, и бесплатный тест на 2 минуты, который покажет, "
    "какая способность у тебя раскроется первой 🤍\n\n"
    f"Смотреть видео или пройти тест в телеграм-канале ⬇️\n\n{CHANNEL_WELCOME_LINK}\n\n"
    "Как дойду до тебя — напишу лично 🪽"
)

# Текст для группы "способности" — утверждено.
AUTO_REPLY_DM_TEXT_SPOSOBNOSTI = (
    "Привет 🤍 Способности есть у каждого — вопрос только, какие уже "
    "раскрыты у тебя 🪽\n\n"
    "Загляни сюда, ссылка в телеграм-канал: там видео про скрытые "
    "способности и бесплатный тест на 2 минуты покажут, какая "
    "сверхспособность раскрывается у тебя ⬇️\n\n"
    f"{CHANNEL_WELCOME_LINK}\n\n"
    "Во благо 🤍🪽"
)

AUTO_REPLY_DM_TEXT_CHAKRY = (
    "Привет 🪬 Бонусная практика по запуску чакр.\n\n"
    "Запускайте чакры через день и вы увидите, как наполнитесь энергией, "
    "ваша жизнь будет постепенно изменяться, во всех сферах вашей жизни, "
    "нервные окончания начнут регенерацию, а внутренние органы будут "
    "исцеляться! 🤍🪽\n\n"
    f"Смотреть видео практики запуска чакр в телеграм-канале ⬇️\n\n{CHAKRY_LINK}\n\n"
    "Во благо 🤍🪽"
)

# Переопределение через переменную окружения — влияет только на текст
# группы "послание" (для обратной совместимости с прежней настройкой).
AUTO_REPLY_DM_TEXT_POSLANIE = os.getenv("AUTO_REPLY_DM_TEXT", AUTO_REPLY_DM_TEXT_POSLANIE)

DM_TEXTS = {
    "poslanie": AUTO_REPLY_DM_TEXT_POSLANIE,
    "sposobnosti": AUTO_REPLY_DM_TEXT_SPOSOBNOSTI,
    "chakry": AUTO_REPLY_DM_TEXT_CHAKRY,
}

# Оставлено для возможного будущего использования, если Meta добавит
# поддержку кнопок для Instagram API с Instagram Login — сейчас не
# применяется, т.к. Button Template не рендерится в этом типе интеграции.
DM_BUTTON_TITLE = os.getenv("DM_BUTTON_TITLE", "Смотреть 🦋")

# ---------------------------------------------------------------------------
# Ответы в комментариях — список вариантов, бот выбирает один случайно,
# чтобы под разными Reels не было одинакового текста подряд (не выглядит
# как спам-бот, и Instagram не начинает подозревать автоматизацию).
# ---------------------------------------------------------------------------
COMMENT_REPLY_VARIANTS = [
    "Записал тебя, загляни в Direct 🤍🪽",
    "Ответил тебе в личку 🤍🪽",
    "Ты в очереди, проверь Direct 🤍🪽",
    "Написал тебе в личные сообщения 🤍🪽",
    "Всё записал, подробности в Direct 🤍🪽",
    "Уже ответил в личку 🤍🪽",
    "Ответил в Direct 🤍🪽",
    "Добавил тебя в список, смотри Direct 🤍🪽",
    "Отправил сообщение в личку 🤍🪽",
    "Ты записан, ответ ждёт в Direct 🤍🪽",
]

if not VERIFY_TOKEN or not PAGE_ACCESS_TOKEN or not IG_USER_ID:
    raise RuntimeError(
        "Не заданы IG_VERIFY_TOKEN и/или IG_PAGE_ACCESS_TOKEN и/или IG_USER_ID. "
        "Проверьте переменные окружения"
    )
